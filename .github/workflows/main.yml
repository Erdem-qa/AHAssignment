name: Cucumber API tests

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Verify Java version
        run: java -version

      - name: Set up Maven
        run: mvn -v

      - name: Cache Maven packages
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven

      - name: Run Cucumber Tests and Generate Reports
        run: mvn test

#      - name: Debug - Check JSON Report
#        run: ls -l target/json-reports/
#
#      - name: Run Maven Verify Step (Generate HTML Reports)
#        run: mvn verify

#      - name: Upload Cucumber Reports
#        uses: actions/upload-artifact@v3
#        with:
#            name: cucumber-reports
#            path: target/json-reports/
#      - name: Upload Cucumber HTML & PDF Reports
#        uses: actions/upload-artifact@v3
#        with:
#          name: cucumber-html-pdf-reports
#          path: target/cucumber-html-reports/
#
#      - name: Upload Extent Spark Reports
#        uses: actions/upload-artifact@v3
#        with:
#          name: extent-spark-reports
#          path: target/spark-reports/
#
#      - name: Upload Extent Reports
#        uses: actions/upload-artifact@v3
#        with:
#          name: extent-reports
#          path: target/extent-report/